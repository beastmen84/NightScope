# Purpose: Define the repository Python lint baseline.
# Contract: Target Python 3.12 and reject selected syntax, import, and name errors.

target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]
